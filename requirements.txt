# Runtime + test dependencies. Python 3.9+.
# Install into a venv:  python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
fastapi==0.128.8
uvicorn==0.39.0
pytest==8.4.2

# (pydantic 2.x and starlette are pulled in transitively by fastapi)
