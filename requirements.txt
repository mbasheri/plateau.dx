# Python dependencies for the Vercel serverless function (api/index.py) and
# local dev/tests. Vercel installs this at the repo root when building /api.
#
# Install locally:  python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
fastapi==0.128.8
psycopg[binary]>=3.1,<4     # Postgres driver (binary wheel, no system libpq needed)
uvicorn==0.39.0             # local dev server only (Vercel provides the runtime)
pytest==8.4.2              # engine test suite only

# (pydantic 2.x and starlette are pulled in transitively by fastapi)
